# Implementation Plan: Blocked Users Page, Conversation Menu, Profile Sheet Report

## 1. Blocked Users Page at `/profile/me`

### Create new page: `src/app/profile/me/blocked/page.tsx`
- Simple page wrapping `<BlockedUsersList />` component (already built)
- Add a back button or use the profile sheet pattern

### Update `src/app/profile/me/_components/my-profile-client.tsx`
- Add "Blocked Users" link in the **Privacy** section (below Discoverable toggle)
- Link to `/profile/me/blocked`
- Show a `ChevronRight` icon (same pattern as Preferences links)

---

## 2. Conversation Menu — Add Block & Report

### Update `src/app/chats/_components/conversation.tsx`
- Add "Block user" menu item (with `Ban` icon, red styling)
- Add "Report user" menu item (with `Flag` icon)
- Both open confirmation dialogs before executing
- After block: navigate away from conversation or show message
- After report: show success toast

### Create report hook: `src/hooks/reports/use-report-user.ts`
- Wrap `useReportsControllerCreateReport` mutation
- Invalidate relevant queries, show toast

---

## 3. Profile Sheet — Add Report Button

### Update `src/components/mobile/profile-sheet.tsx`
- Add a `Flag` icon button **next to the user's name** (line 93-94 area)
- Small, subtle icon (text-muted-foreground) that opens a report dialog
- Report dialog: reason selector (enum from ReportReason) + optional description textarea
- On submit: call `useReportUser` mutation

---

## Files to Create/Modify

| Action | File |
|--------|------|
| Create | `src/app/profile/me/blocked/page.tsx` |
| Modify | `src/app/profile/me/_components/my-profile-client.tsx` |
| Modify | `src/app/chats/_components/conversation.tsx` |
| Create | `src/hooks/reports/use-report-user.ts` |
| Modify | `src/components/mobile/profile-sheet.tsx` |

---

## Verification

1. `npx tsc --noEmit` passes
2. Blocked users page renders at `/profile/me/blocked`
3. Conversation menu shows Block & Report options
4. Profile sheet shows report icon next to name
5. Report dialog submits correctly with reason + description
